---
import ProjectCard from "@/components/astro-ui-components/ProjectCard.astro";
import { projects } from "@/data/projects";
---

<div class="w-full grid grid-cols-1 md:grid-cols-2 gap-6 px-4 mx-auto max-w-6xl">
  {projects.map((project) => <ProjectCard project={project} />)}
</div>
